# Actionable hints for VPN markdown reports (next steps / gap closure).
# Keys must match configs/framework/questions.yaml ids. Optional one-line strings.
version: "1"
hints:
  IDENTITY-001: "Run with fingerprint + YourInfo probes enabled; compare `fingerprint_snapshot` and `yourinfo_snapshot` in normalized.json. See RUN-STEPS.md (benchmark phases)."
  IDENTITY-006: "Browser `services_contacted` is partial; for app auth traffic use external capture or vendor docs (D)."
  IDENTITY-009: "Enable fingerprint capture; without it re-ID risk stays unassessed."
  SIGNUP-001: "Set `competitor_probe` + `surface_urls` for signup/checkout in the provider YAML; re-run `vpn-leaks run`."
  SIGNUP-004: "Same as signup surface — competitor web HAR and `har_summary.json`."
  SIGNUP-010: "Enable competitor web probes; check `cdn_headers` / `web_probes` in competitor_surface."
  WEB-001: "Set `competitor_probe.provider_domains` (and related probes); for desk truth use `dig apex NS` + glue WHOIS (see docs/research-questions-and-evidence.md §H)."
  WEB-004: "Enable web/portal probes; headers show CDN/WAF signals. Compare with desk `curl -I` if needed."
  WEB-008: "Enable competitor probes and review HAR / redirects in raw artifacts."
  DNS-001: "Review `dns_servers_observed` tiers in normalized.json; add locations for diversity."
  DNS-002: "Compare resolver IPs to exit; read `dns_leak_notes` (heuristic). Capture baseline off-VPN if comparing."
  DNS-003: "Same as DNS-002; transition tests help — run with `--transition-tests` when supported."
  DNS-004: "Run `vpn-leaks run` with `--transition-tests` (see RUN-STEPS.md)."
  DNS-009: "Inspect raw DNS captures / resolver lists; DoH/DoT may not be isolated in summary alone."
  DNS-011: "Attribute resolver IPs (O); compare to exit ASN (I/D)."
  IP-001: "If leak flags false, IPv4 aligns with tunnel; add more locations to generalize."
  IP-002: "Enable IPv6 path in environment; check `ipv6/` artifacts when present."
  IP-006: "Review `webrtc_candidates` and `webrtc_leak_flag` in raw webrtc probes."
  IP-007: "Inspect host vs srflx candidates in `webrtc_candidates`."
  IP-014: "Compare `exit_ip_sources` entries for disagreement."
  CTRL-002: "Expand `services_contacted` via full harness; list is harness-visible URLs only."
  CTRL-003: "DOCUMENT_RESEARCH: vendor docs, app MITM, or support (D)."
  CTRL-004: "Classify `services_contacted` hosts; app telemetry needs traffic capture (see TELEM-*)."
  CTRL-009: "Enable portal/web probes (`portal_probes`); check `https_cdn_headers`."
  EXIT-001: "Add more `locations` in provider YAML and re-run for per-region exits."
  EXIT-002: "Use `attribution.json` / merged ASN; add RIR desk checks if needed (S)."
  EXIT-003: "Same as EXIT-002; PeeringDB/RIPE excerpts in attribution when present."
  EXIT-004: "Check raw `exit_dns.json` / attribution for rDNS when stored."
  EXIT-005: "Compare `extra.exit_geo` to `vpn_location_label`; add more regions to validate."
  THIRDWEB-001: "Enable `competitor_probe` + marketing URLs; scripts listed in `web_probes.json`."
  THIRDWEB-003: "HAR + `har_summary.json` tracker_candidates when competitor probes run."
  THIRDWEB-012: "Review HAR for Set-Cookie; summary may be partial."
  FP-001: "Enable browserleaks / fingerprint phase; without data FP questions stay open."
  FP-011: "Harness exercises WebRTC on leak-test pages; provider marketing pages need separate probe config."
  TELEM-001: "INTERNAL_UNVERIFIABLE in harness; use binary/network analysis or vendor disclosures (D)."
  TELEM-004: "Same as TELEM-001."
  OS-001: "Process-level bypass not in default harness; external tooling or manual checks."
  FAIL-001: "Use `--transition-tests` for connect-phase leaks when supported."
  FAIL-003: "Use `--transition-tests` for reconnect leaks when supported."
  FAIL-004: "Crash/kill scenarios not in default harness; fault injection or manual test."
  LOG-001: "Review `services_contacted` + endpoint classifications; pair with policy/audit (D)."
  LOG-005: "Fetch policies (`policy_urls` in provider YAML); compare marketing to ISAE/DPAs (D). See docs/research-questions-and-evidence.md."
